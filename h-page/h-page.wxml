<!--components/h-page/h-page.wxml-->
<view class="h-page h-page--{{ page }}" style="{{ style }}">
  <view wx:if="{{ navbarBorder }}" class="navbar-border"></view>
  <slot></slot>
  <view wx:if="{{ tabbarBorder }}" class="tabbar-border"></view>
</view>
